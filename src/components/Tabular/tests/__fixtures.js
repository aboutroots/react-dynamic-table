const filters = {
  sortBy: 'createdAt',
  sortDirection: 'desc',
  userNameFilter: '',
  itemsOnPage: 5,
  currentPage: 0,
};

const altFilters = {
  sortBy: 'id',
  sortDirection: 'asc',
  userNameFilter: 'b',
  itemsOnPage: 10,
  currentPage: 0,
};

export { filters, altFilters };
